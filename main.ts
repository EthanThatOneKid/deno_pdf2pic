// import { flags, imagemagick } from "deno_pdf2pic/deps.ts";

if (import.meta.main) {
  await main();
}

async function main() {
  // TODO:
  // - Parse input and output file names from command line arguments.
  // - Convert PDF to PNG.
}
